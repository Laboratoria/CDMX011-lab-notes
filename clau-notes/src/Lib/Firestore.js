// import { db } from "../Secrets";

// export const saveNote = (text) =>
// db.collection('note').doc().set({
//     text,
// });

// export const update = (callback) => db.collection('note').onSnapshot(callback);
// export const deleteNote = (id) => db.collection('note').doc(id).delete();